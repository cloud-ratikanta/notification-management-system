package com.interview.assessment.notification.strategy.config;

/**
 * Placeholder configuration removed in favor of component-scanned strategies.
 * The application now picks up concrete `ChannelDeliveryStrategy` beans and
 * registers them using the `ChannelStrategyRegistry` component. This file is
 * intentionally kept empty to avoid accidental bean duplication.
 */
public final class StrategyAutoConfiguration {
    private StrategyAutoConfiguration() { }
}

