package com.interview.assessment.notification.strategy;

/**
 * Deprecated placeholder left for backwards compatibility of earlier drafts.
 * The project now uses `ChannelStrategyRegistry` (a component) backed by
 * discovered `ChannelDeliveryStrategy` beans. This class is intentionally
 * left empty to avoid compile-time conflicts with the primary registry.
 */
@Deprecated
public final class InMemoryChannelStrategyRegistry {
    private InMemoryChannelStrategyRegistry() { }
}

