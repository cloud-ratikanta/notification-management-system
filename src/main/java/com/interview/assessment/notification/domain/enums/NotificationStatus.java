package com.interview.assessment.notification.domain.enums;

public enum NotificationStatus {
    ACCEPTED,
    ROUTED,
    QUEUED,
    IN_PROGRESS,
    COMPLETED,
    FAILED,
    REJECTED,
    DUPLICATE,
    EXPIRED
}

